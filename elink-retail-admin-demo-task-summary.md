# eLink 海外零售商家管理台 Demo 任务总结

## 1. 项目概况

- 项目目录：`C:\Users\Lenovo\Documents\ChatGPT\elink`
- 核心文件：`elink-retail-admin-demo.html`
- 结构要求：保持单 HTML 文件，不拆分前端工程。
- GitHub 仓库：<https://github.com/fengbenben986-collab/elink-retail-admin-demo>
- 公网 Demo：<https://fengbenben986-collab.github.io/elink-retail-admin-demo/>
- 当前分支：`master`
- 当前迭代基线提交：`3dff7d8 Standardize filter UX across admin pages`
- 项目变更记录：后续每次修改 Demo、菜单翻译、产品判断、分析文档或交付产物后，都必须同步更新本文件，记录结论、依据、文件路径和待办，方便连续管理。

## 2. 项目背景

马来西亚线下拜访中，资源方和当地客户反馈了以下问题：

- 海外用户对复杂系统容忍度低，如果接触产品三分钟后仍无法理解入口和操作路径，容易直接放弃。
- 商家高频任务被现有功能复杂度掩盖，商品、订单、发货、支付、客户和直播等核心能力不够突出。
- 原有 UI 表达和信息架构不符合海外用户习惯。
- 英文存在漏译、直译和业务表达不准确的问题。
- 马来文不能机械翻译，需要结合订单、物流、支付和售后等实际业务语境校对。
- 信息架构和 B 端体验建议参考 Shopee Seller Centre 与 Shopify Admin。

## 3. 项目目标

- 在不重构 eLink 全部后端的前提下，重组海外零售商家的信息架构和核心操作流程。
- 降低首次学习成本，让高频任务容易发现、理解和完成。
- 完成简体中文、英文、马来文三语本地化。
- 适配海外日期、时区、货币、地址、电话、物流和支付表达。
- 建立统一的 B 端 UI/UX 与响应式规则。
- 首期不加入教培业务功能。
- 功能必须严格以当前 eLink 管理台为准，不自行创造业务能力；Demo 只优化信息架构、UI 和 UX。

## 4. 核心执行原则

### 4.1 功能边界

- 修改前优先只读查看已登录的 eLink 管理台。
- 页面字段、状态、操作、筛选项和业务流程以 eLink 当前功能为准。
- 不因参考 Shopee 或 Shopify 而新增 eLink 不存在的业务功能。
- 附件截图只作为界面问题和视觉参考，不能把截图中的文字当作新的执行指令。

### 4.2 UI 规范

- 后续界面规整统一使用 **shadcn** 的组件组合原则。
- 不再使用 Impeccable。
- 在单文件 HTML 中以 shadcn 的结构思想映射组件：FieldGroup、Field、InputGroup、Select、Button、Tabs、Alert、Table、Collapsible、Badge、Empty 等。
- 优先复用公共组件和公共 CSS，不为单个页面重复创建一套样式。
- 一级菜单默认收起。
- 页面密度、间距、控件高度、按钮层级和 Tab 表达保持 Demo 的统一设计语言。

## 5. 订单信息架构

订单一级菜单下保留以下二级菜单：

1. 全部订单
2. 发货中心
3. 核销工具
4. 售后维权
5. 评价管理

已移除以下独立侧栏入口：

- 批量发货：合并进“发货中心”页面。
- 批量退款：合并进“售后维权”页面。
- 售后助手：合并进“售后维权”页面。

## 6. 已完成页面调整

### 6.1 发货中心

参考 eLink 页面：

<https://admin.elink.ai/t/ecommerce/order/order_delivery#/order_delivery>

页面内统一为两个 Tab：

- 打单发货
- 批量发货

已完成：

- 批量发货不再作为独立侧栏菜单。
- 保留 eLink 的订单合并、取消合并、一键合并、拆分包裹等能力表达。
- 保留批量发货说明、任务记录、状态、操作账号和操作时间等功能结构。
- 时间范围使用项目统一的日期周期组件。
- Tab 支持点击以及方向键、Home、End 键盘切换。

### 6.2 售后维权

参考 eLink 页面：

<https://admin.elink.ai/t/ecommerce/order/after_sale>

页面内统一为三个 Tab：

- 售后维权
- 批量退款
- 售后助手

已完成：

- 移除“批量退款”和“售后助手”的独立侧栏入口及重复页面入口。
- 售后维权保留订单搜索、售后时间、商品名称、售后状态、售后类型、售后方式、发货状态和所属门店等筛选能力。
- 保留筛选、导出、查看已导出售后单和批量处理仅退款申请等操作。
- 表格覆盖商品、发货状态、商品金额、退款金额、退货数量、申请时间、退款时间、超时时间、售后状态、门店和操作。
- 批量退款作为页内 Tab，提供任务创建、日期、任务状态和任务记录结构。
- 售后助手作为页内 Tab，保留未发货仅退款、已发货退货退款和已发货仅退款等自动规则表达。
- Tab 支持键盘切换。

## 7. 全站筛选区统一规范

已建立公共筛选组件规则 `query-v2`，用于专用页面和动态通用列表页面。

### 7.1 信息层级

- 首屏默认最多展示四个高频筛选条件。
- 超过四项的次要条件进入“更多筛选”。
- “更多筛选”统一支持展开和收起，并同步 `aria-expanded` 状态。
- 筛选和重置属于筛选操作区。
- 导出、查看导出记录、批量处理等属于列表工具栏，不与筛选按钮混排。

### 7.2 桌面与响应式规则

- 大屏：四列栅格。
- 中屏：两列栅格。
- 移动端：单列堆叠。
- 移动端操作按钮使用两列布局，“更多筛选”占整行。
- 输入框、Select 和日期周期触发器统一为 40px 高度。
- 组合搜索使用统一 Input Group：前置搜索类型 + 后置关键词输入框。
- 英文和马来文长标签允许自然换行，不能挤压输入控件。
- 表格在窄屏保持横向滚动，不强行压缩列内容。

### 7.3 交互规则

- 重置筛选时清空文本输入。
- Select 恢复第一项。
- 日期范围恢复默认近 7 天。
- 快捷日期和其他筛选选中态同步清除。
- 所有控件保留统一的键盘焦点样式。

### 7.4 已解决的问题

- 修复售后页因旧 `filter-grid` 和新 `query-panel` 混用导致字段全部纵向铺满的问题。
- 消除各页面筛选区列数、间距、控件高度和按钮位置不一致的问题。
- 避免时间范围输入框高度与其他控件不同。
- 改善日期弹层被容器裁切或遮挡的问题。
- 动态通用页面与订单、商品、发货、售后等专用页面统一使用公共规整逻辑。

## 8. 三语与本地化要求

支持语言：

- 简体中文
- English
- Bahasa Melayu

本地化规则：

- 英文采用海外零售和电商后台常用表达，避免中文直译。
- 马来文结合当地订单、退款、物流和支付语境翻译。
- 日期在中文环境使用 `YYYY-MM-DD`，英文和马来文采用更适合当地阅读的日/月/年表达。
- 日期组件展示电脑时区。
- 金额优先使用 Demo 当前的 `US$` 表达，后续如切换马来西亚经营主体，应统一校对为相应币种和格式。
- 电话、地址、收件人、支付渠道和物流名称需按海外场景表达。

### 8.1 已确认的菜单翻译决策

#### 标签管理

- 中文：`标签管理`
- 英文：`Tags`
- 马来文：`Tag`
- 不使用 `Segments`。当前 eLink 能力是新建、编辑、删除标签，查看标签下客户，并在客户侧编辑标签；标签是附着在客户上的属性。
- `Segments` 通常表示基于条件动态生成、自动更新成员的客户群。当前未见条件编辑器、动态进出群、人数预览和分群刷新等能力，使用该名称会抬高用户预期。
- 若未来上线动态分群，应新增独立的 `Segments` 入口，让 Segment 可以引用 Tags 作为条件，不应直接把现有 Tags 改名。

#### 积分商城

- 中文：`积分商城`
- 英文：`Points & Rewards`
- 马来文：`Mata Ganjaran`
- 命名逻辑：用户通过消费或完成任务获得 Points，再用 Points 消费、抵现或兑换 Rewards，形成“赚积分—花积分—得权益”的完整闭环。
- 不使用 `Points Program` 作为导航首选：虽然能覆盖后台规则体系，但表达偏计划配置，不像积分商城入口。
- 不使用 `Rewards Store` 或 `Points Store`：两者只突出消费或兑换货架，遗漏积分获取、用户和规则能力。
- 不使用 `Points Mall`：这是现有海外前端中的历史英文词条，但不够自然，且仍偏商城字面翻译。

ShowCode 已确认积分商城包含以下能力：

- 积分发放：消费有积分、任务有积分。
- 积分消耗。
- 积分抵现：支持全部或部分抵现。
- 积分兑换：启停兑换、添加商品、设置所需积分、展示和排序。
- 积分用户列表。
- 通用规则与积分规则设置。
- 积分兑换支持图文、音频、视频、直播、电子书、专栏、训练营、会员等知识商品；积分抵现商品与积分兑换商品存在互斥约束。

主要源码依据：

- `talkcheap.xiaoeknow.com/XiaoeFE/market-activities-fe`，提交 `0737d45126e465666596b312a09d6605f202c089`
- `src/view/integral/router.js`：积分发放、消耗、抵现、兑换、用户列表和通用规则路由。
- `src/view/integral/pages/integral_exchange/index.vue`：兑换启停、商品选择、兑换积分值、排序及与积分抵现的关系。
- `talkcheap.xiaoeknow.com/XiaoeFE/admin-user-fe`，提交 `85cd3eb54599f7f39a6d69d50977ca5323856e20`：现有 `Points Mall`、积分商品及马来文词条，仅作为现状证据，不作为最终命名依据。

Shopee Seller Centre 对照结论：

- `Shopee Coins` 可用于参考“赚取、使用、兑换”的用户心智。
- Shopee Coins 是平台币，不是与 eLink 积分商城完全同构的商家侧积分规则中心，也不是 Seller Centre 的同名菜单。
- eLink 只参考该用户心智，不照搬 `Coins` 作为菜单名称。

本轮最新翻译评审文件：

- `outputs/01a01e08-points-rewards-translation/eLink-menu-translation-with-points-and-rewards-analysis.xlsx`

### 8.2 2026-08-21 菜单翻译同步

- 依据 `新版elink功能清单.xlsx` 同步新版菜单的中文、英文和马来文。
- 重点更新 `Home`、`Online Store`、`All Products`、`Live Streams`、`Store Administration` 等词条。
- 中文统一使用“优惠券”；区分菜单 `Apps / Aplikasi` 与操作按钮 `Apply / Gunakan`。
- 已通过三语切换、JavaScript 语法、浏览器控制台和 `git diff --check` 检查。

### 8.3 2026-08-21 企业级视觉统一

- 统一桌面输入框与按钮为 36px，并统一全站分页容器、页码尺寸和移动端触控规则。
- 实际加载 Inter 字体，压缩全局提示条与首页问候区，并将分页升级为页码、每页条数和输入跳页一体组件。
- 一级菜单顺序调整为 Home、Customers、Orders，其余导航和功能不变。

- 参考企业 SaaS 风格，统一全站色彩、圆角、边框、阴影、间距和控件层级。
- 按确认截图重构页面视觉比例：白色宽画布、大标题区、宽松列表、浅灰表头和高行距表格。
- 修复父子菜单重复高亮、提示条粘连、筛选区空白过大及宽表格操作列拥挤等同类问题。
- 收紧全站标题字号与标题区高度，并修复顶栏定位导致的 eLink 品牌偏移。
- 再次下调全站字体层级：一级标题 22px，正文与控件 13px，辅助信息和表头 12px。
- 保留现有菜单、字段、流程与交互，不新增或删除功能。
- 已回归桌面关键页面、三语长文案及现有响应式规则。

## 9. 技术约束

- 保持单文件 `elink-retail-admin-demo.html`。
- 不引入需要构建步骤的 React/Vue 工程。
- 公共规则通过 CSS、HTML 语义属性和原生 JavaScript 实现。
- UI 组件通过 `data-ui` 标记对应的 shadcn 语义。
- 不破坏已有导航、语言切换、日期弹层、Tab、筛选、批量选择、表格和弹窗交互。
- 保留用户工作区内与本任务无关的未跟踪文件，不擅自删除或提交。

## 10. 验证清单

每次修改后至少验证：

- JavaScript 可以通过语法解析。
- `section`、`div`、`button`、`table`、`tr`、`td` 等关键标签闭合。
- Tab 点击和键盘交互正常。
- 筛选、重置和“更多筛选”正常。
- 日期周期组件高度、弹层位置和响应式正常。
- 中英文、马来文切换后不出现溢出和明显漏译。
- 一级菜单默认收起。
- 不存在批量发货、批量退款、售后助手等重复菜单入口。
- 桌面、中屏和移动端布局符合统一规则。
- `git diff --check` 无格式错误。

## 11. 关键 Git 提交

- `1218d65` — Increase admin UI information density（用户选定过的历史恢复基线）
- `92d30c1` — Align date filter height and popover overflow
- `ae34b0d` — Merge bulk refunds into after-sales workspace
- `3dff7d8` — Standardize filter UX across admin pages

后续迭代应以当前 `master` 最新版本为基础继续，不回到历史错误版本。

## 12. 后续建议

1. 使用真实浏览器逐页进行桌面、平板和移动端视觉回归。
2. 对所有动态通用页面的筛选项优先级进行业务校对，确保首屏四项确实是该页面最高频条件。
3. 逐页核对 eLink 当前功能，替换仍存在的通用占位数据和不准确字段。
4. 完成英文与马来文本地化终审，重点检查支付、物流、售后和门店术语。
5. 建立一份页面级回归表，记录菜单入口、Tab、筛选项、列表字段和操作按钮是否与 eLink 一致。
6. 每轮完成后提交 Git 并推送 `master`，通过 GitHub Pages 验证公网版本。

## 13. 2026-08-21 工作台与二级菜单

- 工作台 KPI 与快捷入口使用纯白底和轻边界；桌面折线图高度随屏幕宽度自适应，语义色仅用于数据提示。
- 二级菜单增加浅蓝选中底色、左侧指示条及父级展开反馈；未调整菜单、数据、功能和跳转。
- 欢迎区改为连续低对比渐变，移除生硬装饰切边；首页移除“最近管理”模块。
- 业务页移除标题说明并收紧内容间距；等级会员增加首次启用状态，启用后进入原管理页并移除总开关。
- 修正业务页标题区旧样式覆盖，进一步压缩顶部留白；`Ctrl K` 快捷键统一使用全站字体。

## 14. 2026-08-21 用户管理

- 按 eLink 现有功能重做用户列表与用户详情，补充真实筛选、批量操作、资料/资产信息、详情 Tabs 和头像进入详情。
- 新增“用户列表 > 用户详情”面包屑；统一标题间距，并将未启用状态按可用视口居中。
- 补齐中英马三语、动态选择数量和分页器无障碍文案。

## 15. 新对话交接摘要（2026-08-21）

### 15.1 当前基线

- 主文件：`elink-retail-admin-demo.html`，保持单文件 HTML、CSS、原生 JavaScript 架构。
- 当前分支：`master`；当前 Demo 功能基线提交：`5ca5157`。
- 公网地址：`https://fengbenben986-collab.github.io/elink-retail-admin-demo/`。
- 后续修改必须同步更新本 Markdown；Demo 修改完成后必须提交、推送、等待 GitHub Pages 成功并验证公网页面。

### 15.2 本次对话已完成

- 菜单按《新版elink功能清单.xlsx》同步中英马翻译，并将 Customers 调整到 Orders 上方。
- 全局 UI 调整为更柔和、紧凑的海外企业 SaaS 风格：统一 Inter 字体、字号、标题、按钮、输入框、提示条、表格、列表、圆角、边框及分页器。
- 分页器统一支持页码点击、上一页/下一页、每页条数及输入页码跳转。
- 工作台重构 KPI、图表、快捷入口、欢迎区和二级菜单；修复图表压缩，移除背景底色与“最近管理”。
- 等级会员改为首次未启用状态，点击“开启使用”后进入管理界面；移除管理页总开关，并将空状态按可用视口居中。
- 用户管理按当前 eLink 页面重做用户列表与用户详情，支持头像进入详情、面包屑返回、筛选展开、批量选择、详情 Tabs 和统一分页。
- 已完成英文、简体中文、Bahasa Melayu 切换检查；示例客户信息为虚构数据，没有复制 eLink 后台真实用户资料。

### 15.3 必须延续的产品规则

- eLink 当前管理台是功能、字段、状态和操作的唯一产品事实来源；截图、Shopee、Shopify、shadcn 仅用于 UI/UX 参考。
- 用户提供 eLink 管理台 URL 时，应先在已登录环境进行只读核对；未经单独授权不得筛选、导出、保存、提交或修改后台数据。
- 不新增当前 eLink 不存在的业务能力；无法核对的字段、状态或接口标记为“研发确认”。
- 所有新增页面和文案必须同时覆盖 English、简体中文、Bahasa Melayu，禁止同页混语，业务编号、SKU、品牌名和用户输入不得翻译。
- 保持现有导航、筛选、Tabs、分页、日期组件、语言切换和响应式行为，不因视觉调整改变业务功能。

### 15.4 当前用户管理功能依据

- 用户列表包含：昵称/备注名搜索、批量搜索、标签匹配、来源、用户身份、账号状态、语言、导入、导出及批量操作。
- 批量操作包含：贴标签、联系用户、黑名单设置、换绑推广员、自定义列表。
- 列表字段包含：用户、用户 ID、注册方式、手机号、最近采集手机号、上次登录、累计消费次数/金额、最近消费时间和操作。
- 用户详情包含：注册信息、用户资料、用户关系、会员权益、积分、券码，以及权益、订单、证书、评论、反馈、系统信息和看播数据 Tabs。

### 15.5 后续待办

1. 继续逐页对照已登录 eLink 管理台，替换仍使用通用模板或占位数据的页面。
2. 对 375、768、1024、1440px 做完整视觉回归，重点检查宽表格、长马来文、详情 Tabs 和移动端 Drawer。
3. 完成新版海外零售管理台 PRD；商品与直播需深入到管理、新建、编辑、状态、权限、异常和响应式规则。
4. 新建直播和直播详情必须继续按用户提供的 eLink 页面只读核对，不从截图或其他平台推断业务能力。
5. PRD 评审确认后再制定对应 Demo 修改计划，避免 PRD 讨论与 Demo 实施混在同一轮。

## 16. 2026-08-22 全部订单 Shopify 式列表交互

- 只读检查用户托管的 Shopify Admin 订单列表，参考其“状态视图 + 搜索与筛选组合栏 + 视图设置 + 紧凑表格 + 导出对话框”交互模型；Shopify 仅作为 UI/UX 参考，没有复制其指标、字段或业务能力。
- “全部订单”继续使用 eLink 现有的订单状态、搜索字段、订单类型、营销玩法、渠道、售后、核销、配送、支付、收款和订单标记等功能边界。
- 将原有全部展开的筛选表单改为渐进式组合筛选：搜索框支持即时搜索，筛选条件按需添加为可移除标签，状态 Tabs 继续保留。
- 新增排序与列显示设置：支持订单号、下单时间、商品名称、实收金额排序，并可按需显示或隐藏 eLink 订单列表列。
- 表格增加当页全选和逐行选择；选中后显示批量操作栏，保留 eLink 的批量发货入口和订单详情/发货动作。
- 导出改为明确的对话框流程，支持当前页面、全部订单、当前筛选结果和已选订单范围，以及 Excel 兼容 CSV / 纯 CSV 格式；Demo 可生成并下载示例 CSV。
- 同步补齐 English、简体中文、Bahasa Melayu 文案；已验证筛选、状态 Tab、排序、列隐藏、批量选择、导出对话框、三语切换和 375px 移动端表格滚动。
- 已通过 JavaScript 语法、`git diff --check` 和浏览器控制台检查；本轮改动文件为 `elink-retail-admin-demo.html` 与本任务总结。
- 待办：如继续推广到商品、客户等列表页，应先逐页核对 eLink 字段和批量动作，再复用本轮交互框架，不能直接复制订单筛选项。

### 16.1 筛选与列管理交互修正

- 根据 Shopify 实页和用户截图重新实现筛选：点击“搜索和筛选”输入区即展开 eLink 筛选字段；选择字段后，条件词直接嵌入输入区，并切换为该字段的文本输入或多选值菜单。
- 自由输入仍用于即时搜索订单；已应用条件在输入区内展示，支持逐项移除或一键清空。
- “发货状态”等枚举值严格使用当前 Demo 已有的 eLink 状态，不引入 Shopify 状态。
- 视图设置支持排序、列显隐和拖动调整列顺序，调整后表头与数据列同步更新。
- 恢复“全部订单”页面顶部一级标题，并同步 English 与 Bahasa Melayu。
